# Retrieval self-improvement (code-space loop) — PLACEHOLDER, not implemented.
#
# The code-space autoresearch loop (an agent that edits retrieval blocks,
# gated by tests + the benchmark, opening a PR) is NOT built yet. Its design
# and the real CI skeleton live in improve.md and issue #1.
#
# This file is only a manually-dispatchable placeholder so the workflow has a
# home. It does NO real work and always succeeds — it must never apt/pip/build,
# so it can't fail or send failure emails. Replace the single step below with
# the skeleton from improve.md when actually wiring up the loop.

name: autoresearch (placeholder)

on:
  workflow_dispatch: {}   # manual only; never on push/schedule

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Not implemented yet
        run: |
          echo "The code-space autoresearch loop is not implemented yet."
          echo "Design + CI skeleton: see improve.md and issue #1."
